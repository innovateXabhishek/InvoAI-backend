# Python dependencies for the Invo AI backend.  Pin versions to
# ensure reproducible deployments.  These packages are used to build
# a high‑performance, type‑hinted API server, perform asynchronous
# processing, interact with a relational database, schedule background
# tasks and work with environment variables.

fastapi==0.111.0
uvicorn==0.22.0
pydantic==1.10.15
SQLAlchemy==2.0.29
psycopg2-binary==2.9.9
python-dotenv==1.0.1
python-multipart==0.0.9
aiofiles==23.1.0
celery==5.3.5
redis==5.0.0
httpx==0.27.0  # HTTP client for calling external APIs (e.g. OpenAI)
